name: axiom-street

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-street}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-street}
      POSTGRES_DB: ${POSTGRES_DB:-street}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-street} -d ${POSTGRES_DB:-street}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.api
    environment:
      STREET_DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-street}:${POSTGRES_PASSWORD:-street}@postgres:5432/${POSTGRES_DB:-street}
      STREET_REDIS_URL: redis://redis:6379/0
      STREET_CELERY_BROKER_URL: redis://redis:6379/0
      STREET_CELERY_RESULT_BACKEND: redis://redis:6379/1
      STREET_DATA_ROOT: /data
      STREET_JOBS_ROOT: /jobs
      STREET_LEAN_IMAGE: quantconnect/lean:16355
      STREET_CORS_ORIGINS: http://localhost:3000
      STREET_SYNC_BACKTESTS: "false"
      STREET_SYNC_INGESTS: "false"
      STREET_MAX_INFLIGHT_BACKTESTS: "2"
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./jobs:/jobs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 10

  worker:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.api
    command: celery -A services.worker.celery_app worker --beat --loglevel=INFO --concurrency=2
    environment:
      STREET_DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-street}:${POSTGRES_PASSWORD:-street}@postgres:5432/${POSTGRES_DB:-street}
      STREET_REDIS_URL: redis://redis:6379/0
      STREET_CELERY_BROKER_URL: redis://redis:6379/0
      STREET_CELERY_RESULT_BACKEND: redis://redis:6379/1
      STREET_DATA_ROOT: "${PWD}/data"
      STREET_JOBS_ROOT: "${PWD}/jobs"
      STREET_LEAN_IMAGE: quantconnect/lean:16355
      STREET_WORKER_CONCURRENCY: "2"
      STREET_LEAN_TIMEOUT_SECONDS: "1800"
      DOCKER_HOST: unix:///var/run/docker.sock
    volumes:
      # Bind the host paths at the same location so `docker run -v` from the
      # worker can see job files (Docker daemon resolves paths on the host/VM).
      - "${PWD}/data:${PWD}/data"
      - "${PWD}/jobs:${PWD}/jobs"
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "celery -A services.worker.celery_app inspect ping -d celery@$$HOSTNAME --timeout 5 | grep -q pong"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  web:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.web
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_healthy

volumes:
  pgdata:
